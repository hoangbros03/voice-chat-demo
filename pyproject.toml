[project]
name = "voice-chat-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=2.15.0",
]

[tool.uv.workspace]
members = [
    "fastrtc_core",
    "services/fastrtc_core",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
